# Ground State Seating — 課題提出用プロジェクト

座席配置最適化ツール（静電ポテンシャルのアナロジーで「エネルギー的に一番落ち着く」座席配置を計算する）の、課題提出用ワークスペース。

- GitHubリポジトリ: https://github.com/gqsunflower/Ground-State-Seating
- 開発の背景・経緯・設計判断は一つ上の階層 `../README.md` と `../DEVELOPMENT_LOG.md` にまとまっている。**このフォルダで作業する前に一読推奨**（特に「なぜ単純な合算モデルではダメだったか」の部分）
- 元となる実装は `../seat_optimizer.py`。このフォルダでは、それを踏まえた提出用の成果物（整理済みコード・レポート・実行結果など）を作成していく

## Git運用ルール

このフォルダは Git リポジトリとして初期化済み。以下のルールを守ること。

1. **コードやファイルを変更するたびに、GitHubへプッシュする**（変更を溜め込まない。1つの意味のある変更ごとにコミット→プッシュ）
2. **プッシュする前に、アップロードすべきでない情報が含まれていないか必ず確認する**
   - 実名・個人が特定できる情報、メールアドレスなどの連絡先、社外秘・非公開のデータ
   - APIキー・トークン・パスワードなどの認証情報
   - 確認は `git status` / `git diff --staged` で変更内容を目視してから `git push` する
3. **アップロードすべきでない情報は、Gitの管理対象から外し、別管理にする**
   - そのようなファイル・フォルダは `.gitignore` に追記してコミット対象から除外する
   - 例: 実名入りのアンケート生データ、個人を特定できる元データなど → `data/private/` のようなフォルダに置き、`.gitignore` で除外する
   - 座席最適化の入力データは、人物名を `A, B, C...` のような記号・匿名ラベルに置き換えたものだけをコミット対象にする（`../data/original_data.xlsx` は既にこの形式）
4. 上記に反する内容を誤ってコミットしてしまった場合は、追加コミットで消すだけでなく、リモートへの反映前であれば履歴からも取り除いてから対応する（相談の上で対応）

## ファイル構成の方針

- 1つの成果物 = 目的が分かるファイル名で作成する（例: コードは `seat_optimizer.py`、レポートは `report.md` など）
- 外部ライブラリ・ビルド環境に依存しすぎない、シンプルな構成を保つ（元プロジェクトの方針を踏襲）
